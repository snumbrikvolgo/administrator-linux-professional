---
- name: start nginx
  ansible.builtin.systemd:
    name: nginx
    state: started
    enabled: true

- name: restart nginx
  ansible.builtin.systemd:
    name: nginx
    state: restarted
    enabled: true
