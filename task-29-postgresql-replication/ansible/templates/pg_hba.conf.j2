local all all peer
host all all 127.0.0.1/32 scram-sha-256
host all all ::1/128 scram-sha-256
local replication all peer
host replication all 127.0.0.1/32 scram-sha-256
host replication all ::1/128 scram-sha-256
host replication {{ replicator_user }} {{ master_ip }}/32 scram-sha-256
host replication {{ replicator_user }} {{ slave_ip }}/32 scram-sha-256
host all {{ barman_db_user }} {{ barman_ip }}/32 scram-sha-256
host replication {{ barman_db_user }} {{ barman_ip }}/32 scram-sha-256
